import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#1E1E1E',
        flexDirection: 'row',
        gap: 3,
        alignItems: 'center',
    },
    title: {
        color: '#fff',
        fontSize: 12,
        fontWeight: 600,
    }
})